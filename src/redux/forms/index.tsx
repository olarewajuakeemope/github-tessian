const asFormDetails = (name: string, fields: string[]) => ({ name, fields })

export const userSearchForm = asFormDetails('userSearchForm', [])
export const commitSearchForm = asFormDetails('commitSearchForm', [])
